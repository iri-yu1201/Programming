Git (ターミナル、bash)のメモ

・mkdir（新規ディレクトリを作成）
mkdir フォルダ名

・rm（ファイルやディレクトリを削除）
rm ファイル名

rm -r -f ディレクトリ名（フォルダ名）
	ディレクトリの中身を（-r）強制的に削除する（-f）意味

・mv（ファイルやディレクトリを移動）
mv 移動元のファイル(ディレクトリ) 移動先のパス

・cp（ファイルやディレクトリをコピー）
cp コピー元のファイル(ディレクトリ) コピー先のパス